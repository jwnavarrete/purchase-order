import React from 'react'
import {Button} from '@mui/material';

const Actions = () => {
    return (
        <Button variant="contained" size="small">Generar pedido</Button>
    )
}

export default Actions